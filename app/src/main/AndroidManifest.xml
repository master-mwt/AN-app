<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.univaq.disim.mwt.android_native_app">
    <!-- PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- ACTIVITIES -->
        <activity android:name=".ExploreActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchActivity" />
        <activity android:name=".TvShowDetailsActivity" />
        <activity android:name=".UserCollectionActivity" />
        <activity android:name=".SeasonActivity" />
        <activity android:name=".EpisodeActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".AuthActivity" />
        <activity android:name=".InfoActivity" />

        <!-- TEMP ACTIVITIES -->
        <activity android:name=".AuthSignUpActivity" />
        <activity android:name=".AuthSignInActivity" />
        <!-- /TEMP ACTIVITIES -->

        <!-- SERVICES -->
        <service android:name=".services.DataParserService" />
        <service android:name=".services.UserCollectionService" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>